{
  "name": "top",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {},
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "@types/node": "^24.10.0"
  },
  "dependencies": {
    "@faker-js/faker": "^10.1.0",
    "builder-pattern": "^2.2.0"
  },
  "scripts": {
    "test": "playwright test",
    "ui": "playwright test --ui",
    "pipeline": "npx playwright test --grep \"@pipeline\" --workers=1 --project=chromium --reporter=line,junit"
  }
}